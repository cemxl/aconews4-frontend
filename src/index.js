import React from 'react';
import ReactDOM from 'react-dom';
import App from './app.js';  // Import app from src


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
